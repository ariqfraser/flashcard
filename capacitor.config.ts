import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "com.ariqfraser.flashcard",
    appName: "flashcard",
    webDir: "dist/flashcard/browser",
};

export default config;
