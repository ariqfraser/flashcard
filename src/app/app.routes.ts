import { Routes } from "@angular/router";
import { DemoFeature } from "@features/demo-feature/demo-feature";

export const routes: Routes = [
    {
        path: "",
        component: DemoFeature,
    },
];
